/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.resources.groups.members.sync;

import com.vitablehealth.connect.core.ClientOptions;
import com.vitablehealth.connect.core.RequestOptions;
import com.vitablehealth.connect.resources.groups.members.sync.requests.GetSyncRequest;
import com.vitablehealth.connect.resources.groups.members.sync.requests.GroupMemberSyncRequest;
import com.vitablehealth.connect.types.GroupMemberSyncDetailResponse;
import com.vitablehealth.connect.types.GroupMemberSyncRequestDetailResponse;
import java.util.concurrent.CompletableFuture;

public class AsyncSyncClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawSyncClient rawClient;

    public AsyncSyncClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawSyncClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawSyncClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Submits a member sync payload for the specified group. Members in the payload will be queued for processing asynchronously. Returns HTTP 202 with the batch ID and acceptance timestamp.
     */
    public CompletableFuture<GroupMemberSyncDetailResponse> submit(String groupId, GroupMemberSyncRequest request) {
        return this.rawClient.submit(groupId, request).thenApply(response -> response.body());
    }

    /**
     * Submits a member sync payload for the specified group. Members in the payload will be queued for processing asynchronously. Returns HTTP 202 with the batch ID and acceptance timestamp.
     */
    public CompletableFuture<GroupMemberSyncDetailResponse> submit(
            String groupId, GroupMemberSyncRequest request, RequestOptions requestOptions) {
        return this.rawClient.submit(groupId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves a previously-submitted group member sync request by its <code>grpmsr_</code> ID. Returns the acceptance timestamp, completion timestamp (if processing has finished), and the per-member <code>results</code> once available. While processing is in flight, <code>completed_at</code> and <code>results</code> are <code>null</code>.
     */
    public CompletableFuture<GroupMemberSyncRequestDetailResponse> get(String groupId, String requestId) {
        return this.rawClient.get(groupId, requestId).thenApply(response -> response.body());
    }

    /**
     * Retrieves a previously-submitted group member sync request by its <code>grpmsr_</code> ID. Returns the acceptance timestamp, completion timestamp (if processing has finished), and the per-member <code>results</code> once available. While processing is in flight, <code>completed_at</code> and <code>results</code> are <code>null</code>.
     */
    public CompletableFuture<GroupMemberSyncRequestDetailResponse> get(
            String groupId, String requestId, RequestOptions requestOptions) {
        return this.rawClient.get(groupId, requestId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves a previously-submitted group member sync request by its <code>grpmsr_</code> ID. Returns the acceptance timestamp, completion timestamp (if processing has finished), and the per-member <code>results</code> once available. While processing is in flight, <code>completed_at</code> and <code>results</code> are <code>null</code>.
     */
    public CompletableFuture<GroupMemberSyncRequestDetailResponse> get(
            String groupId, String requestId, GetSyncRequest request) {
        return this.rawClient.get(groupId, requestId, request).thenApply(response -> response.body());
    }

    /**
     * Retrieves a previously-submitted group member sync request by its <code>grpmsr_</code> ID. Returns the acceptance timestamp, completion timestamp (if processing has finished), and the per-member <code>results</code> once available. While processing is in flight, <code>completed_at</code> and <code>results</code> are <code>null</code>.
     */
    public CompletableFuture<GroupMemberSyncRequestDetailResponse> get(
            String groupId, String requestId, GetSyncRequest request, RequestOptions requestOptions) {
        return this.rawClient.get(groupId, requestId, request, requestOptions).thenApply(response -> response.body());
    }
}

/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.resources.enrollments;

import com.vitablehealth.connect.core.ClientOptions;
import com.vitablehealth.connect.core.RequestOptions;
import com.vitablehealth.connect.resources.enrollments.requests.GetEnrollmentsRequest;
import com.vitablehealth.connect.resources.enrollments.requests.ReissueEnrollmentRequest;
import com.vitablehealth.connect.resources.enrollments.requests.TerminateEnrollmentRequest;
import com.vitablehealth.connect.types.EnrollmentResponse;
import com.vitablehealth.connect.types.ReissueEnrollmentResponse;
import java.util.concurrent.CompletableFuture;

public class AsyncEnrollmentsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawEnrollmentsClient rawClient;

    public AsyncEnrollmentsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawEnrollmentsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawEnrollmentsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Retrieves a single enrollment: the employee and employer it belongs to, the benefit product, its status, the coverage period, the employee payroll deduction and employer contribution, and the enrolled plan's Summary of Benefits and Coverage document when one is on file. An enrollment the caller cannot reach is indistinguishable from one that does not exist.
     */
    public CompletableFuture<EnrollmentResponse> get(String enrollmentId) {
        return this.rawClient.get(enrollmentId).thenApply(response -> response.body());
    }

    /**
     * Retrieves a single enrollment: the employee and employer it belongs to, the benefit product, its status, the coverage period, the employee payroll deduction and employer contribution, and the enrolled plan's Summary of Benefits and Coverage document when one is on file. An enrollment the caller cannot reach is indistinguishable from one that does not exist.
     */
    public CompletableFuture<EnrollmentResponse> get(String enrollmentId, RequestOptions requestOptions) {
        return this.rawClient.get(enrollmentId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves a single enrollment: the employee and employer it belongs to, the benefit product, its status, the coverage period, the employee payroll deduction and employer contribution, and the enrolled plan's Summary of Benefits and Coverage document when one is on file. An enrollment the caller cannot reach is indistinguishable from one that does not exist.
     */
    public CompletableFuture<EnrollmentResponse> get(String enrollmentId, GetEnrollmentsRequest request) {
        return this.rawClient.get(enrollmentId, request).thenApply(response -> response.body());
    }

    /**
     * Retrieves a single enrollment: the employee and employer it belongs to, the benefit product, its status, the coverage period, the employee payroll deduction and employer contribution, and the enrolled plan's Summary of Benefits and Coverage document when one is on file. An enrollment the caller cannot reach is indistinguishable from one that does not exist.
     */
    public CompletableFuture<EnrollmentResponse> get(
            String enrollmentId, GetEnrollmentsRequest request, RequestOptions requestOptions) {
        return this.rawClient.get(enrollmentId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Closes the targeted enrollment and creates a new unanswered enrollment for the same member and plan year. VPC never requires a qualifying life event; other products require an accepted, member-owned event outside open enrollment. User-backed callers must provide a reason; it is optional for organization API-key callers. Tenant mismatches return a non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<ReissueEnrollmentResponse> reissue(String enrollmentId) {
        return this.rawClient.reissue(enrollmentId).thenApply(response -> response.body());
    }

    /**
     * Closes the targeted enrollment and creates a new unanswered enrollment for the same member and plan year. VPC never requires a qualifying life event; other products require an accepted, member-owned event outside open enrollment. User-backed callers must provide a reason; it is optional for organization API-key callers. Tenant mismatches return a non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<ReissueEnrollmentResponse> reissue(String enrollmentId, RequestOptions requestOptions) {
        return this.rawClient.reissue(enrollmentId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Closes the targeted enrollment and creates a new unanswered enrollment for the same member and plan year. VPC never requires a qualifying life event; other products require an accepted, member-owned event outside open enrollment. User-backed callers must provide a reason; it is optional for organization API-key callers. Tenant mismatches return a non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<ReissueEnrollmentResponse> reissue(String enrollmentId, ReissueEnrollmentRequest request) {
        return this.rawClient.reissue(enrollmentId, request).thenApply(response -> response.body());
    }

    /**
     * Closes the targeted enrollment and creates a new unanswered enrollment for the same member and plan year. VPC never requires a qualifying life event; other products require an accepted, member-owned event outside open enrollment. User-backed callers must provide a reason; it is optional for organization API-key callers. Tenant mismatches return a non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<ReissueEnrollmentResponse> reissue(
            String enrollmentId, ReissueEnrollmentRequest request, RequestOptions requestOptions) {
        return this.rawClient.reissue(enrollmentId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Terminates enrolled coverage immediately. An accepted qualifying life event owned by the enrollment member is required unless the plan is VPC or ICHRA. User-backed callers must provide a reason; it is optional for organization API-key callers. API keys may act across the caller organization's book. Tenant mismatches return the same non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<Void> terminate(String enrollmentId) {
        return this.rawClient.terminate(enrollmentId).thenApply(response -> response.body());
    }

    /**
     * Terminates enrolled coverage immediately. An accepted qualifying life event owned by the enrollment member is required unless the plan is VPC or ICHRA. User-backed callers must provide a reason; it is optional for organization API-key callers. API keys may act across the caller organization's book. Tenant mismatches return the same non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<Void> terminate(String enrollmentId, RequestOptions requestOptions) {
        return this.rawClient.terminate(enrollmentId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Terminates enrolled coverage immediately. An accepted qualifying life event owned by the enrollment member is required unless the plan is VPC or ICHRA. User-backed callers must provide a reason; it is optional for organization API-key callers. API keys may act across the caller organization's book. Tenant mismatches return the same non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<Void> terminate(String enrollmentId, TerminateEnrollmentRequest request) {
        return this.rawClient.terminate(enrollmentId, request).thenApply(response -> response.body());
    }

    /**
     * Terminates enrolled coverage immediately. An accepted qualifying life event owned by the enrollment member is required unless the plan is VPC or ICHRA. User-backed callers must provide a reason; it is optional for organization API-key callers. API keys may act across the caller organization's book. Tenant mismatches return the same non-disclosing 404 before the request body is validated.
     */
    public CompletableFuture<Void> terminate(
            String enrollmentId, TerminateEnrollmentRequest request, RequestOptions requestOptions) {
        return this.rawClient.terminate(enrollmentId, request, requestOptions).thenApply(response -> response.body());
    }
}

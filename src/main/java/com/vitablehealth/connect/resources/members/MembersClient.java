/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.resources.members;

import com.vitablehealth.connect.core.ClientOptions;
import com.vitablehealth.connect.core.RequestOptions;
import com.vitablehealth.connect.core.pagination.SyncPagingIterable;
import com.vitablehealth.connect.resources.members.requests.GetHouseholdMembersRequest;
import com.vitablehealth.connect.resources.members.requests.GetMembersRequest;
import com.vitablehealth.connect.resources.members.requests.ListDependentsMembersRequest;
import com.vitablehealth.connect.resources.members.requests.ListEmploymentsMembersRequest;
import com.vitablehealth.connect.resources.members.requests.ListEnrollmentsMembersRequest;
import com.vitablehealth.connect.resources.members.requests.ListIdCardsMembersRequest;
import com.vitablehealth.connect.resources.members.requests.ListMembersRequest;
import com.vitablehealth.connect.resources.members.requests.ListQualifyingLifeEventsMembersRequest;
import com.vitablehealth.connect.types.HouseholdMembersResponse;
import com.vitablehealth.connect.types.MemberDependentsResponse;
import com.vitablehealth.connect.types.MemberDigitalBenefitCardsResponse;
import com.vitablehealth.connect.types.MemberEmploymentsResponse;
import com.vitablehealth.connect.types.MemberEnrollmentsResponse;
import com.vitablehealth.connect.types.MemberListItem;
import com.vitablehealth.connect.types.MemberQualifyingLifeEvent;
import com.vitablehealth.connect.types.MemberResponse;

public class MembersClient {
    protected final ClientOptions clientOptions;

    private final RawMembersClient rawClient;

    public MembersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawMembersClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawMembersClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Retrieves a member's profile by ID — identity, demographics, address, contact details, tobacco status, and profile status. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberResponse get(String memberId) {
        return this.rawClient.get(memberId).body();
    }

    /**
     * Retrieves a member's profile by ID — identity, demographics, address, contact details, tobacco status, and profile status. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberResponse get(String memberId, RequestOptions requestOptions) {
        return this.rawClient.get(memberId, requestOptions).body();
    }

    /**
     * Retrieves a member's profile by ID — identity, demographics, address, contact details, tobacco status, and profile status. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberResponse get(String memberId, GetMembersRequest request) {
        return this.rawClient.get(memberId, request).body();
    }

    /**
     * Retrieves a member's profile by ID — identity, demographics, address, contact details, tobacco status, and profile status. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberResponse get(String memberId, GetMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.get(memberId, request, requestOptions).body();
    }

    /**
     * Lists a member's active legal dependents — name, relationship, date of birth, age, and sex at birth. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberDependentsResponse listDependents(String memberId) {
        return this.rawClient.listDependents(memberId).body();
    }

    /**
     * Lists a member's active legal dependents — name, relationship, date of birth, age, and sex at birth. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberDependentsResponse listDependents(String memberId, RequestOptions requestOptions) {
        return this.rawClient.listDependents(memberId, requestOptions).body();
    }

    /**
     * Lists a member's active legal dependents — name, relationship, date of birth, age, and sex at birth. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberDependentsResponse listDependents(String memberId, ListDependentsMembersRequest request) {
        return this.rawClient.listDependents(memberId, request).body();
    }

    /**
     * Lists a member's active legal dependents — name, relationship, date of birth, age, and sex at birth. Access is scoped to the authenticated principal; a member not visible to the caller returns a 404.
     */
    public MemberDependentsResponse listDependents(
            String memberId, ListDependentsMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.listDependents(memberId, request, requestOptions).body();
    }

    /**
     * Lists a member's employment across every employer — the same employee record shape as the employer's employees list, plus the employer name. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all employments. A member not visible to the caller returns a 404.
     */
    public MemberEmploymentsResponse listEmployments(String memberId) {
        return this.rawClient.listEmployments(memberId).body();
    }

    /**
     * Lists a member's employment across every employer — the same employee record shape as the employer's employees list, plus the employer name. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all employments. A member not visible to the caller returns a 404.
     */
    public MemberEmploymentsResponse listEmployments(String memberId, RequestOptions requestOptions) {
        return this.rawClient.listEmployments(memberId, requestOptions).body();
    }

    /**
     * Lists a member's employment across every employer — the same employee record shape as the employer's employees list, plus the employer name. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all employments. A member not visible to the caller returns a 404.
     */
    public MemberEmploymentsResponse listEmployments(String memberId, ListEmploymentsMembersRequest request) {
        return this.rawClient.listEmployments(memberId, request).body();
    }

    /**
     * Lists a member's employment across every employer — the same employee record shape as the employer's employees list, plus the employer name. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all employments. A member not visible to the caller returns a 404.
     */
    public MemberEmploymentsResponse listEmployments(
            String memberId, ListEmploymentsMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.listEmployments(memberId, request, requestOptions).body();
    }

    /**
     * Lists a member's benefit enrollments across every employer — benefit type and product, employer, carrier, plan, tier, employee deduction, employer contribution and total premium, the individual enrollment coverage boundary (<code>coverage_end</code>), the separate pre-effective cancellation boundary (<code>cancelled_date</code>), and the distinct benefit plan-year boundary (<code>plan_year_coverage_end</code>) used to determine whether the plan year itself has ended, the date the enrollment record was created (<code>issued_date</code>, the value Ops labels Issued on, reported for every row whatever the member answered), the window the member could answer in -- which never opens before the enrollment was issued, so a row issued mid-open-enrollment starts its window on its issue date -- whether a qualifying life event would currently be required for reissue under the product/open-enrollment rule, enrollment/open-enrollment window, and two statuses: <code>election_status</code> (what the member answered) and <code>policy_status</code> (what became of their coverage, null unless they enrolled). Every row includes a stable enrollment ID and the exact employer and benefit plan-year IDs used to fetch that row's plan-year detail. The full list is returned across all states so the client derives active plans (effective and upcoming) and the enrollment history from those per-row statuses. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all enrollments. A member not visible to the caller returns a 404.
     */
    public MemberEnrollmentsResponse listEnrollments(String memberId) {
        return this.rawClient.listEnrollments(memberId).body();
    }

    /**
     * Lists a member's benefit enrollments across every employer — benefit type and product, employer, carrier, plan, tier, employee deduction, employer contribution and total premium, the individual enrollment coverage boundary (<code>coverage_end</code>), the separate pre-effective cancellation boundary (<code>cancelled_date</code>), and the distinct benefit plan-year boundary (<code>plan_year_coverage_end</code>) used to determine whether the plan year itself has ended, the date the enrollment record was created (<code>issued_date</code>, the value Ops labels Issued on, reported for every row whatever the member answered), the window the member could answer in -- which never opens before the enrollment was issued, so a row issued mid-open-enrollment starts its window on its issue date -- whether a qualifying life event would currently be required for reissue under the product/open-enrollment rule, enrollment/open-enrollment window, and two statuses: <code>election_status</code> (what the member answered) and <code>policy_status</code> (what became of their coverage, null unless they enrolled). Every row includes a stable enrollment ID and the exact employer and benefit plan-year IDs used to fetch that row's plan-year detail. The full list is returned across all states so the client derives active plans (effective and upcoming) and the enrollment history from those per-row statuses. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all enrollments. A member not visible to the caller returns a 404.
     */
    public MemberEnrollmentsResponse listEnrollments(String memberId, RequestOptions requestOptions) {
        return this.rawClient.listEnrollments(memberId, requestOptions).body();
    }

    /**
     * Lists a member's benefit enrollments across every employer — benefit type and product, employer, carrier, plan, tier, employee deduction, employer contribution and total premium, the individual enrollment coverage boundary (<code>coverage_end</code>), the separate pre-effective cancellation boundary (<code>cancelled_date</code>), and the distinct benefit plan-year boundary (<code>plan_year_coverage_end</code>) used to determine whether the plan year itself has ended, the date the enrollment record was created (<code>issued_date</code>, the value Ops labels Issued on, reported for every row whatever the member answered), the window the member could answer in -- which never opens before the enrollment was issued, so a row issued mid-open-enrollment starts its window on its issue date -- whether a qualifying life event would currently be required for reissue under the product/open-enrollment rule, enrollment/open-enrollment window, and two statuses: <code>election_status</code> (what the member answered) and <code>policy_status</code> (what became of their coverage, null unless they enrolled). Every row includes a stable enrollment ID and the exact employer and benefit plan-year IDs used to fetch that row's plan-year detail. The full list is returned across all states so the client derives active plans (effective and upcoming) and the enrollment history from those per-row statuses. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all enrollments. A member not visible to the caller returns a 404.
     */
    public MemberEnrollmentsResponse listEnrollments(String memberId, ListEnrollmentsMembersRequest request) {
        return this.rawClient.listEnrollments(memberId, request).body();
    }

    /**
     * Lists a member's benefit enrollments across every employer — benefit type and product, employer, carrier, plan, tier, employee deduction, employer contribution and total premium, the individual enrollment coverage boundary (<code>coverage_end</code>), the separate pre-effective cancellation boundary (<code>cancelled_date</code>), and the distinct benefit plan-year boundary (<code>plan_year_coverage_end</code>) used to determine whether the plan year itself has ended, the date the enrollment record was created (<code>issued_date</code>, the value Ops labels Issued on, reported for every row whatever the member answered), the window the member could answer in -- which never opens before the enrollment was issued, so a row issued mid-open-enrollment starts its window on its issue date -- whether a qualifying life event would currently be required for reissue under the product/open-enrollment rule, enrollment/open-enrollment window, and two statuses: <code>election_status</code> (what the member answered) and <code>policy_status</code> (what became of their coverage, null unless they enrolled). Every row includes a stable enrollment ID and the exact employer and benefit plan-year IDs used to fetch that row's plan-year detail. The full list is returned across all states so the client derives active plans (effective and upcoming) and the enrollment history from those per-row statuses. For an organization caller the rows are scoped to companies in that organization's book; a member (self/household) or Vitable Admin sees all enrollments. A member not visible to the caller returns a 404.
     */
    public MemberEnrollmentsResponse listEnrollments(
            String memberId, ListEnrollmentsMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.listEnrollments(memberId, request, requestOptions).body();
    }

    /**
     * Lists a member's household as a per-participant table — the account holder plus each active household member, with name, relationship, member type, date of birth, and household-admin flag. Access is scoped to the authenticated principal; a member not visible to the caller (or with no household) returns a 404.
     */
    public HouseholdMembersResponse getHousehold(String memberId) {
        return this.rawClient.getHousehold(memberId).body();
    }

    /**
     * Lists a member's household as a per-participant table — the account holder plus each active household member, with name, relationship, member type, date of birth, and household-admin flag. Access is scoped to the authenticated principal; a member not visible to the caller (or with no household) returns a 404.
     */
    public HouseholdMembersResponse getHousehold(String memberId, RequestOptions requestOptions) {
        return this.rawClient.getHousehold(memberId, requestOptions).body();
    }

    /**
     * Lists a member's household as a per-participant table — the account holder plus each active household member, with name, relationship, member type, date of birth, and household-admin flag. Access is scoped to the authenticated principal; a member not visible to the caller (or with no household) returns a 404.
     */
    public HouseholdMembersResponse getHousehold(String memberId, GetHouseholdMembersRequest request) {
        return this.rawClient.getHousehold(memberId, request).body();
    }

    /**
     * Lists a member's household as a per-participant table — the account holder plus each active household member, with name, relationship, member type, date of birth, and household-admin flag. Access is scoped to the authenticated principal; a member not visible to the caller (or with no household) returns a 404.
     */
    public HouseholdMembersResponse getHousehold(
            String memberId, GetHouseholdMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.getHousehold(memberId, request, requestOptions).body();
    }

    /**
     * Lists a member's benefit ID cards — card type (medical, dental, vision, or rx), employer, plan, provider network, claims payer, carrier contact details, and the disclaimers printed on the card. Medical, dental and vision cards come from the member's active digital benefit cards; the rx card from the member's Ventegra pharmacy benefit (omitted when the member has no free-medication coverage), which carries no plan, network, or carrier details. Access is scoped to the authenticated principal, and an organization caller sees only cards from employers in its book; a member not visible to the caller returns a 404.
     */
    public MemberDigitalBenefitCardsResponse listIdCards(String memberId) {
        return this.rawClient.listIdCards(memberId).body();
    }

    /**
     * Lists a member's benefit ID cards — card type (medical, dental, vision, or rx), employer, plan, provider network, claims payer, carrier contact details, and the disclaimers printed on the card. Medical, dental and vision cards come from the member's active digital benefit cards; the rx card from the member's Ventegra pharmacy benefit (omitted when the member has no free-medication coverage), which carries no plan, network, or carrier details. Access is scoped to the authenticated principal, and an organization caller sees only cards from employers in its book; a member not visible to the caller returns a 404.
     */
    public MemberDigitalBenefitCardsResponse listIdCards(String memberId, RequestOptions requestOptions) {
        return this.rawClient.listIdCards(memberId, requestOptions).body();
    }

    /**
     * Lists a member's benefit ID cards — card type (medical, dental, vision, or rx), employer, plan, provider network, claims payer, carrier contact details, and the disclaimers printed on the card. Medical, dental and vision cards come from the member's active digital benefit cards; the rx card from the member's Ventegra pharmacy benefit (omitted when the member has no free-medication coverage), which carries no plan, network, or carrier details. Access is scoped to the authenticated principal, and an organization caller sees only cards from employers in its book; a member not visible to the caller returns a 404.
     */
    public MemberDigitalBenefitCardsResponse listIdCards(String memberId, ListIdCardsMembersRequest request) {
        return this.rawClient.listIdCards(memberId, request).body();
    }

    /**
     * Lists a member's benefit ID cards — card type (medical, dental, vision, or rx), employer, plan, provider network, claims payer, carrier contact details, and the disclaimers printed on the card. Medical, dental and vision cards come from the member's active digital benefit cards; the rx card from the member's Ventegra pharmacy benefit (omitted when the member has no free-medication coverage), which carries no plan, network, or carrier details. Access is scoped to the authenticated principal, and an organization caller sees only cards from employers in its book; a member not visible to the caller returns a 404.
     */
    public MemberDigitalBenefitCardsResponse listIdCards(
            String memberId, ListIdCardsMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.listIdCards(memberId, request, requestOptions).body();
    }

    /**
     * Lists a member's qualifying life events, including events already used for another enrollment. Returns all statuses by default; pass the status query param to filter to one (e.g. approved). Events are ordered newest submission first with stable paging. Custom text is present only when submitted and is otherwise null. A member not visible to the caller returns a 404. API keys and unbound access tokens have organization-wide access. Employer-bound tokens require employment at the bound employer, and employee-bound tokens require the exact employee-member relationship. Organization or scope mismatches return a 404 before pagination is validated.
     */
    public SyncPagingIterable<MemberQualifyingLifeEvent> listQualifyingLifeEvents(String memberId) {
        return this.rawClient.listQualifyingLifeEvents(memberId).body();
    }

    /**
     * Lists a member's qualifying life events, including events already used for another enrollment. Returns all statuses by default; pass the status query param to filter to one (e.g. approved). Events are ordered newest submission first with stable paging. Custom text is present only when submitted and is otherwise null. A member not visible to the caller returns a 404. API keys and unbound access tokens have organization-wide access. Employer-bound tokens require employment at the bound employer, and employee-bound tokens require the exact employee-member relationship. Organization or scope mismatches return a 404 before pagination is validated.
     */
    public SyncPagingIterable<MemberQualifyingLifeEvent> listQualifyingLifeEvents(
            String memberId, RequestOptions requestOptions) {
        return this.rawClient.listQualifyingLifeEvents(memberId, requestOptions).body();
    }

    /**
     * Lists a member's qualifying life events, including events already used for another enrollment. Returns all statuses by default; pass the status query param to filter to one (e.g. approved). Events are ordered newest submission first with stable paging. Custom text is present only when submitted and is otherwise null. A member not visible to the caller returns a 404. API keys and unbound access tokens have organization-wide access. Employer-bound tokens require employment at the bound employer, and employee-bound tokens require the exact employee-member relationship. Organization or scope mismatches return a 404 before pagination is validated.
     */
    public SyncPagingIterable<MemberQualifyingLifeEvent> listQualifyingLifeEvents(
            String memberId, ListQualifyingLifeEventsMembersRequest request) {
        return this.rawClient.listQualifyingLifeEvents(memberId, request).body();
    }

    /**
     * Lists a member's qualifying life events, including events already used for another enrollment. Returns all statuses by default; pass the status query param to filter to one (e.g. approved). Events are ordered newest submission first with stable paging. Custom text is present only when submitted and is otherwise null. A member not visible to the caller returns a 404. API keys and unbound access tokens have organization-wide access. Employer-bound tokens require employment at the bound employer, and employee-bound tokens require the exact employee-member relationship. Organization or scope mismatches return a 404 before pagination is validated.
     */
    public SyncPagingIterable<MemberQualifyingLifeEvent> listQualifyingLifeEvents(
            String memberId, ListQualifyingLifeEventsMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .listQualifyingLifeEvents(memberId, request, requestOptions)
                .body();
    }

    /**
     * Retrieves a paginated list of the members in the authenticated organization's book — identity, contact details, and address. The book covers members reached through an employer in the organization's book as well as members of a group it owns. Supports free-text search (name, email, phone number, or exact member id).
     */
    public SyncPagingIterable<MemberListItem> list() {
        return this.rawClient.list().body();
    }

    /**
     * Retrieves a paginated list of the members in the authenticated organization's book — identity, contact details, and address. The book covers members reached through an employer in the organization's book as well as members of a group it owns. Supports free-text search (name, email, phone number, or exact member id).
     */
    public SyncPagingIterable<MemberListItem> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Retrieves a paginated list of the members in the authenticated organization's book — identity, contact details, and address. The book covers members reached through an employer in the organization's book as well as members of a group it owns. Supports free-text search (name, email, phone number, or exact member id).
     */
    public SyncPagingIterable<MemberListItem> list(ListMembersRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * Retrieves a paginated list of the members in the authenticated organization's book — identity, contact details, and address. The book covers members reached through an employer in the organization's book as well as members of a group it owns. Supports free-text search (name, email, phone number, or exact member id).
     */
    public SyncPagingIterable<MemberListItem> list(ListMembersRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }
}

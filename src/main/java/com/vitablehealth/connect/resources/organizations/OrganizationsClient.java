/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.resources.organizations;

import com.vitablehealth.connect.core.ClientOptions;
import com.vitablehealth.connect.core.RequestOptions;
import com.vitablehealth.connect.resources.organizations.requests.CreateOrganizationRequest;
import com.vitablehealth.connect.types.Organization;
import com.vitablehealth.connect.types.OrganizationsListResponse;

public class OrganizationsClient {
    protected final ClientOptions clientOptions;

    private final RawOrganizationsClient rawClient;

    public OrganizationsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawOrganizationsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawOrganizationsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists the organizations the authenticated caller is an active member of (paginated). Returns an empty list when the caller belongs to no organizations.
     */
    public OrganizationsListResponse list() {
        return this.rawClient.list().body();
    }

    /**
     * Lists the organizations the authenticated caller is an active member of (paginated). Returns an empty list when the caller belongs to no organizations.
     */
    public OrganizationsListResponse list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Onboards the authenticated user's partner Organization: creates the local Organization + the creator's admin membership atomically, then mirrors it to WorkOS (creates the WorkOS org and binds the creator as admin). A user may hold several organizations and selects which one a request acts as with the <code>X-Vitable-Organization</code> header. The founder's email domain is claimed only when no other organization holds it, so a taken domain is left with its owner rather than rejected.
     */
    public Organization create(CreateOrganizationRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Onboards the authenticated user's partner Organization: creates the local Organization + the creator's admin membership atomically, then mirrors it to WorkOS (creates the WorkOS org and binds the creator as admin). A user may hold several organizations and selects which one a request acts as with the <code>X-Vitable-Organization</code> header. The founder's email domain is claimed only when no other organization holds it, so a taken domain is left with its owner rather than rejected.
     */
    public Organization create(CreateOrganizationRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }
}

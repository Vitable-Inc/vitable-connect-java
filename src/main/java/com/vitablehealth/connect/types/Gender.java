/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class Gender {
    public static final Gender NON_BINARY = new Gender(Value.NON_BINARY, "Non-binary");

    public static final Gender FEMALE = new Gender(Value.FEMALE, "Female");

    public static final Gender PREFER_NOT_TO_RESPOND = new Gender(Value.PREFER_NOT_TO_RESPOND, "Prefer not to respond");

    public static final Gender MALE = new Gender(Value.MALE, "Male");

    public static final Gender TRANSGENDER = new Gender(Value.TRANSGENDER, "Transgender");

    private final Value value;

    private final String string;

    Gender(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof Gender && this.string.equals(((Gender) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case NON_BINARY:
                return visitor.visitNonBinary();
            case FEMALE:
                return visitor.visitFemale();
            case PREFER_NOT_TO_RESPOND:
                return visitor.visitPreferNotToRespond();
            case MALE:
                return visitor.visitMale();
            case TRANSGENDER:
                return visitor.visitTransgender();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static Gender valueOf(String value) {
        switch (value) {
            case "Non-binary":
                return NON_BINARY;
            case "Female":
                return FEMALE;
            case "Prefer not to respond":
                return PREFER_NOT_TO_RESPOND;
            case "Male":
                return MALE;
            case "Transgender":
                return TRANSGENDER;
            default:
                return new Gender(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        MALE,

        FEMALE,

        TRANSGENDER,

        NON_BINARY,

        PREFER_NOT_TO_RESPOND,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitMale();

        T visitFemale();

        T visitTransgender();

        T visitNonBinary();

        T visitPreferNotToRespond();

        T visitUnknown(String unknownType);
    }
}

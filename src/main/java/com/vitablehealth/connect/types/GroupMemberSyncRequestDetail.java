/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.vitablehealth.connect.core.Nullable;
import com.vitablehealth.connect.core.NullableNonemptyFilter;
import com.vitablehealth.connect.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GroupMemberSyncRequestDetail.Builder.class)
public final class GroupMemberSyncRequestDetail {
    private final String requestId;

    private final String groupId;

    private final OffsetDateTime acceptedAt;

    private final Optional<OffsetDateTime> completedAt;

    private final Optional<GroupMemberSyncResults> results;

    private final Map<String, Object> additionalProperties;

    private GroupMemberSyncRequestDetail(
            String requestId,
            String groupId,
            OffsetDateTime acceptedAt,
            Optional<OffsetDateTime> completedAt,
            Optional<GroupMemberSyncResults> results,
            Map<String, Object> additionalProperties) {
        this.requestId = requestId;
        this.groupId = groupId;
        this.acceptedAt = acceptedAt;
        this.completedAt = completedAt;
        this.results = results;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Identifier for the sync request being reported on.
     */
    @JsonProperty("request_id")
    public String getRequestId() {
        return requestId;
    }

    /**
     * @return Group whose roster was synced (<code>grp_*</code>).
     */
    @JsonProperty("group_id")
    public String getGroupId() {
        return groupId;
    }

    /**
     * @return When Vitable accepted the sync request for processing (RFC 3339).
     */
    @JsonProperty("accepted_at")
    public OffsetDateTime getAcceptedAt() {
        return acceptedAt;
    }

    /**
     * @return When processing finished (RFC 3339), or <code>null</code> while the sync is still in progress.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getCompletedAt() {
        if (completedAt == null) {
            return Optional.empty();
        }
        return completedAt;
    }

    /**
     * @return Outcome of the sync, or <code>null</code> until processing completes.
     */
    @JsonIgnore
    public Optional<GroupMemberSyncResults> getResults() {
        if (results == null) {
            return Optional.empty();
        }
        return results;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("completed_at")
    private Optional<OffsetDateTime> _getCompletedAt() {
        return completedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("results")
    private Optional<GroupMemberSyncResults> _getResults() {
        return results;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof GroupMemberSyncRequestDetail && equalTo((GroupMemberSyncRequestDetail) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(GroupMemberSyncRequestDetail other) {
        return requestId.equals(other.requestId)
                && groupId.equals(other.groupId)
                && acceptedAt.equals(other.acceptedAt)
                && completedAt.equals(other.completedAt)
                && results.equals(other.results);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.requestId, this.groupId, this.acceptedAt, this.completedAt, this.results);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static RequestIdStage builder() {
        return new Builder();
    }

    public interface RequestIdStage {
        /**
         * <p>Identifier for the sync request being reported on.</p>
         */
        GroupIdStage requestId(@NotNull String requestId);

        Builder from(GroupMemberSyncRequestDetail other);
    }

    public interface GroupIdStage {
        /**
         * <p>Group whose roster was synced (<code>grp_*</code>).</p>
         */
        AcceptedAtStage groupId(@NotNull String groupId);
    }

    public interface AcceptedAtStage {
        /**
         * <p>When Vitable accepted the sync request for processing (RFC 3339).</p>
         */
        _FinalStage acceptedAt(@NotNull OffsetDateTime acceptedAt);
    }

    public interface _FinalStage {
        GroupMemberSyncRequestDetail build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When processing finished (RFC 3339), or <code>null</code> while the sync is still in progress.</p>
         */
        _FinalStage completedAt(Optional<OffsetDateTime> completedAt);

        _FinalStage completedAt(OffsetDateTime completedAt);

        _FinalStage completedAt(Nullable<OffsetDateTime> completedAt);

        /**
         * <p>Outcome of the sync, or <code>null</code> until processing completes.</p>
         */
        _FinalStage results(Optional<GroupMemberSyncResults> results);

        _FinalStage results(GroupMemberSyncResults results);

        _FinalStage results(Nullable<GroupMemberSyncResults> results);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements RequestIdStage, GroupIdStage, AcceptedAtStage, _FinalStage {
        private String requestId;

        private String groupId;

        private OffsetDateTime acceptedAt;

        private Optional<GroupMemberSyncResults> results = Optional.empty();

        private Optional<OffsetDateTime> completedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(GroupMemberSyncRequestDetail other) {
            requestId(other.getRequestId());
            groupId(other.getGroupId());
            acceptedAt(other.getAcceptedAt());
            completedAt(other.getCompletedAt());
            results(other.getResults());
            return this;
        }

        /**
         * <p>Identifier for the sync request being reported on.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("request_id")
        public GroupIdStage requestId(@NotNull String requestId) {
            this.requestId = Objects.requireNonNull(requestId, "requestId must not be null");
            return this;
        }

        /**
         * <p>Group whose roster was synced (<code>grp_*</code>).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("group_id")
        public AcceptedAtStage groupId(@NotNull String groupId) {
            this.groupId = Objects.requireNonNull(groupId, "groupId must not be null");
            return this;
        }

        /**
         * <p>When Vitable accepted the sync request for processing (RFC 3339).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("accepted_at")
        public _FinalStage acceptedAt(@NotNull OffsetDateTime acceptedAt) {
            this.acceptedAt = Objects.requireNonNull(acceptedAt, "acceptedAt must not be null");
            return this;
        }

        /**
         * <p>Outcome of the sync, or <code>null</code> until processing completes.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage results(Nullable<GroupMemberSyncResults> results) {
            if (results.isNull()) {
                this.results = null;
            } else if (results.isEmpty()) {
                this.results = Optional.empty();
            } else {
                this.results = Optional.of(results.get());
            }
            return this;
        }

        /**
         * <p>Outcome of the sync, or <code>null</code> until processing completes.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage results(GroupMemberSyncResults results) {
            this.results = Optional.ofNullable(results);
            return this;
        }

        /**
         * <p>Outcome of the sync, or <code>null</code> until processing completes.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "results", nulls = Nulls.SKIP)
        public _FinalStage results(Optional<GroupMemberSyncResults> results) {
            this.results = results;
            return this;
        }

        /**
         * <p>When processing finished (RFC 3339), or <code>null</code> while the sync is still in progress.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage completedAt(Nullable<OffsetDateTime> completedAt) {
            if (completedAt.isNull()) {
                this.completedAt = null;
            } else if (completedAt.isEmpty()) {
                this.completedAt = Optional.empty();
            } else {
                this.completedAt = Optional.of(completedAt.get());
            }
            return this;
        }

        /**
         * <p>When processing finished (RFC 3339), or <code>null</code> while the sync is still in progress.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage completedAt(OffsetDateTime completedAt) {
            this.completedAt = Optional.ofNullable(completedAt);
            return this;
        }

        /**
         * <p>When processing finished (RFC 3339), or <code>null</code> while the sync is still in progress.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "completed_at", nulls = Nulls.SKIP)
        public _FinalStage completedAt(Optional<OffsetDateTime> completedAt) {
            this.completedAt = completedAt;
            return this;
        }

        @java.lang.Override
        public GroupMemberSyncRequestDetail build() {
            return new GroupMemberSyncRequestDetail(
                    requestId, groupId, acceptedAt, completedAt, results, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}

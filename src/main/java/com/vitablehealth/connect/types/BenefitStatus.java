/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class BenefitStatus {
    public static final BenefitStatus UPCOMING = new BenefitStatus(Value.UPCOMING, "upcoming");

    public static final BenefitStatus INACTIVE = new BenefitStatus(Value.INACTIVE, "inactive");

    public static final BenefitStatus OPEN_ENROLLMENT = new BenefitStatus(Value.OPEN_ENROLLMENT, "open_enrollment");

    public static final BenefitStatus ACTIVE = new BenefitStatus(Value.ACTIVE, "active");

    private final Value value;

    private final String string;

    BenefitStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof BenefitStatus && this.string.equals(((BenefitStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case UPCOMING:
                return visitor.visitUpcoming();
            case INACTIVE:
                return visitor.visitInactive();
            case OPEN_ENROLLMENT:
                return visitor.visitOpenEnrollment();
            case ACTIVE:
                return visitor.visitActive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static BenefitStatus valueOf(String value) {
        switch (value) {
            case "upcoming":
                return UPCOMING;
            case "inactive":
                return INACTIVE;
            case "open_enrollment":
                return OPEN_ENROLLMENT;
            case "active":
                return ACTIVE;
            default:
                return new BenefitStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        UPCOMING,

        OPEN_ENROLLMENT,

        INACTIVE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitUpcoming();

        T visitOpenEnrollment();

        T visitInactive();

        T visitUnknown(String unknownType);
    }
}

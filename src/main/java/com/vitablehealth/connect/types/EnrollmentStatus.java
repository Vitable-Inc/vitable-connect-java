/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class EnrollmentStatus {
    public static final EnrollmentStatus PENDING = new EnrollmentStatus(Value.PENDING, "pending");

    public static final EnrollmentStatus INACTIVE = new EnrollmentStatus(Value.INACTIVE, "inactive");

    public static final EnrollmentStatus ENROLLED = new EnrollmentStatus(Value.ENROLLED, "enrolled");

    public static final EnrollmentStatus WAIVED = new EnrollmentStatus(Value.WAIVED, "waived");

    private final Value value;

    private final String string;

    EnrollmentStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof EnrollmentStatus && this.string.equals(((EnrollmentStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PENDING:
                return visitor.visitPending();
            case INACTIVE:
                return visitor.visitInactive();
            case ENROLLED:
                return visitor.visitEnrolled();
            case WAIVED:
                return visitor.visitWaived();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static EnrollmentStatus valueOf(String value) {
        switch (value) {
            case "pending":
                return PENDING;
            case "inactive":
                return INACTIVE;
            case "enrolled":
                return ENROLLED;
            case "waived":
                return WAIVED;
            default:
                return new EnrollmentStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        ENROLLED,

        WAIVED,

        INACTIVE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitEnrolled();

        T visitWaived();

        T visitInactive();

        T visitUnknown(String unknownType);
    }
}

/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.vitablehealth.connect.core.Nullable;
import com.vitablehealth.connect.core.NullableNonemptyFilter;
import com.vitablehealth.connect.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PlanYearEnrollment.Builder.class)
public final class PlanYearEnrollment {
    private final String memberId;

    private final String employeeId;

    private final Optional<String> employeeExternalReferenceId;

    private final String memberFirstName;

    private final String memberLastName;

    private final PlanYearEnrollmentElectionStatus electionStatus;

    private final Optional<PlanYearEnrollmentPolicyStatus> policyStatus;

    private final int dependentCount;

    private final Optional<String> carrier;

    private final Optional<String> plan;

    private final Optional<String> tier;

    private final Optional<Integer> premiumInCents;

    private final Optional<Integer> employerContributionInCents;

    private final Optional<Integer> employeeDeductionInCents;

    private final Map<String, Object> additionalProperties;

    private PlanYearEnrollment(
            String memberId,
            String employeeId,
            Optional<String> employeeExternalReferenceId,
            String memberFirstName,
            String memberLastName,
            PlanYearEnrollmentElectionStatus electionStatus,
            Optional<PlanYearEnrollmentPolicyStatus> policyStatus,
            int dependentCount,
            Optional<String> carrier,
            Optional<String> plan,
            Optional<String> tier,
            Optional<Integer> premiumInCents,
            Optional<Integer> employerContributionInCents,
            Optional<Integer> employeeDeductionInCents,
            Map<String, Object> additionalProperties) {
        this.memberId = memberId;
        this.employeeId = employeeId;
        this.employeeExternalReferenceId = employeeExternalReferenceId;
        this.memberFirstName = memberFirstName;
        this.memberLastName = memberLastName;
        this.electionStatus = electionStatus;
        this.policyStatus = policyStatus;
        this.dependentCount = dependentCount;
        this.carrier = carrier;
        this.plan = plan;
        this.tier = tier;
        this.premiumInCents = premiumInCents;
        this.employerContributionInCents = employerContributionInCents;
        this.employeeDeductionInCents = employeeDeductionInCents;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Our id for the person (<code>mbr_&lt;...&gt;</code>). Stable across every employer they work for.
     */
    @JsonProperty("member_id")
    public String getMemberId() {
        return memberId;
    }

    /**
     * @return Our id for this person's employment with this employer (<code>empl_&lt;...&gt;</code>). A person who leaves and is rehired has two.
     */
    @JsonProperty("employee_id")
    public String getEmployeeId() {
        return employeeId;
    }

    /**
     * @return Your own reference id for this employee, as you supplied it. Null when you have not set one.
     */
    @JsonIgnore
    public Optional<String> getEmployeeExternalReferenceId() {
        if (employeeExternalReferenceId == null) {
            return Optional.empty();
        }
        return employeeExternalReferenceId;
    }

    /**
     * @return The member's first name.
     */
    @JsonProperty("member_first_name")
    public String getMemberFirstName() {
        return memberFirstName;
    }

    /**
     * @return The member's last name.
     */
    @JsonProperty("member_last_name")
    public String getMemberLastName() {
        return memberLastName;
    }

    /**
     * @return Where the member stands: <code>Enrolled</code> or <code>Waived</code> if they answered, otherwise <code>Pending</code> while their enrollment window is open and <code>Expired</code> once it has closed.
     * <ul>
     * <li><code>Enrolled</code> - Enrolled</li>
     * <li><code>Waived</code> - Waived</li>
     * <li><code>Pending</code> - Pending</li>
     * <li><code>Expired</code> - Expired</li>
     * </ul>
     */
    @JsonProperty("election_status")
    public PlanYearEnrollmentElectionStatus getElectionStatus() {
        return electionStatus;
    }

    /**
     * @return Where the member's coverage stands: <code>Coverage Upcoming</code>, <code>Coverage Effective</code>, <code>Coverage Ended</code>, or <code>Cancelled</code>. Null unless the member enrolled.
     * <ul>
     * <li><code>Coverage Upcoming</code> - Coverage Upcoming</li>
     * <li><code>Coverage Effective</code> - Coverage Effective</li>
     * <li><code>Coverage Ended</code> - Coverage Ended</li>
     * <li><code>Cancelled</code> - Cancelled</li>
     * </ul>
     */
    @JsonIgnore
    public Optional<PlanYearEnrollmentPolicyStatus> getPolicyStatus() {
        if (policyStatus == null) {
            return Optional.empty();
        }
        return policyStatus;
    }

    /**
     * @return Dependents covered under this enrollment today. Counts the same dependents <code>premium_in_cents</code> is priced for, so a dependent whose termination is dated in the future still counts.
     */
    @JsonProperty("dependent_count")
    public int getDependentCount() {
        return dependentCount;
    }

    /**
     * @return The carrier for this enrollment: the individual-market carrier for an ICHRA plan, otherwise the benefit's own. Null when the benefit has no carrier.
     */
    @JsonIgnore
    public Optional<String> getCarrier() {
        if (carrier == null) {
            return Optional.empty();
        }
        return carrier;
    }

    /**
     * @return Chosen plan name, or null when unanswered/waived.
     */
    @JsonIgnore
    public Optional<String> getPlan() {
        if (plan == null) {
            return Optional.empty();
        }
        return plan;
    }

    /**
     * @return Chosen coverage tier, or null when unanswered/waived.
     */
    @JsonIgnore
    public Optional<String> getTier() {
        if (tier == null) {
            return Optional.empty();
        }
        return tier;
    }

    /**
     * @return Monthly premium in cents for the chosen plan, dependents included. The plan's own cost, not the employer's share of it. Null when unanswered/waived.
     */
    @JsonIgnore
    public Optional<Integer> getPremiumInCents() {
        if (premiumInCents == null) {
            return Optional.empty();
        }
        return premiumInCents;
    }

    /**
     * @return The employer's monthly share of <code>premium_in_cents</code>, in cents. Null when unanswered/waived.
     */
    @JsonIgnore
    public Optional<Integer> getEmployerContributionInCents() {
        if (employerContributionInCents == null) {
            return Optional.empty();
        }
        return employerContributionInCents;
    }

    /**
     * @return What the employee is deducted monthly, in cents: <code>premium_in_cents</code> less <code>employer_contribution_in_cents</code>, floored at zero. Null when unanswered/waived.
     */
    @JsonIgnore
    public Optional<Integer> getEmployeeDeductionInCents() {
        if (employeeDeductionInCents == null) {
            return Optional.empty();
        }
        return employeeDeductionInCents;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("employee_external_reference_id")
    private Optional<String> _getEmployeeExternalReferenceId() {
        return employeeExternalReferenceId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("policy_status")
    private Optional<PlanYearEnrollmentPolicyStatus> _getPolicyStatus() {
        return policyStatus;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("carrier")
    private Optional<String> _getCarrier() {
        return carrier;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("plan")
    private Optional<String> _getPlan() {
        return plan;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("tier")
    private Optional<String> _getTier() {
        return tier;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("premium_in_cents")
    private Optional<Integer> _getPremiumInCents() {
        return premiumInCents;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("employer_contribution_in_cents")
    private Optional<Integer> _getEmployerContributionInCents() {
        return employerContributionInCents;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("employee_deduction_in_cents")
    private Optional<Integer> _getEmployeeDeductionInCents() {
        return employeeDeductionInCents;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PlanYearEnrollment && equalTo((PlanYearEnrollment) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PlanYearEnrollment other) {
        return memberId.equals(other.memberId)
                && employeeId.equals(other.employeeId)
                && employeeExternalReferenceId.equals(other.employeeExternalReferenceId)
                && memberFirstName.equals(other.memberFirstName)
                && memberLastName.equals(other.memberLastName)
                && electionStatus.equals(other.electionStatus)
                && policyStatus.equals(other.policyStatus)
                && dependentCount == other.dependentCount
                && carrier.equals(other.carrier)
                && plan.equals(other.plan)
                && tier.equals(other.tier)
                && premiumInCents.equals(other.premiumInCents)
                && employerContributionInCents.equals(other.employerContributionInCents)
                && employeeDeductionInCents.equals(other.employeeDeductionInCents);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.memberId,
                this.employeeId,
                this.employeeExternalReferenceId,
                this.memberFirstName,
                this.memberLastName,
                this.electionStatus,
                this.policyStatus,
                this.dependentCount,
                this.carrier,
                this.plan,
                this.tier,
                this.premiumInCents,
                this.employerContributionInCents,
                this.employeeDeductionInCents);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static MemberIdStage builder() {
        return new Builder();
    }

    public interface MemberIdStage {
        /**
         * <p>Our id for the person (<code>mbr_&lt;...&gt;</code>). Stable across every employer they work for.</p>
         */
        EmployeeIdStage memberId(@NotNull String memberId);

        Builder from(PlanYearEnrollment other);
    }

    public interface EmployeeIdStage {
        /**
         * <p>Our id for this person's employment with this employer (<code>empl_&lt;...&gt;</code>). A person who leaves and is rehired has two.</p>
         */
        MemberFirstNameStage employeeId(@NotNull String employeeId);
    }

    public interface MemberFirstNameStage {
        /**
         * <p>The member's first name.</p>
         */
        MemberLastNameStage memberFirstName(@NotNull String memberFirstName);
    }

    public interface MemberLastNameStage {
        /**
         * <p>The member's last name.</p>
         */
        ElectionStatusStage memberLastName(@NotNull String memberLastName);
    }

    public interface ElectionStatusStage {
        /**
         * <p>Where the member stands: <code>Enrolled</code> or <code>Waived</code> if they answered, otherwise <code>Pending</code> while their enrollment window is open and <code>Expired</code> once it has closed.</p>
         * <ul>
         * <li><code>Enrolled</code> - Enrolled</li>
         * <li><code>Waived</code> - Waived</li>
         * <li><code>Pending</code> - Pending</li>
         * <li><code>Expired</code> - Expired</li>
         * </ul>
         */
        DependentCountStage electionStatus(@NotNull PlanYearEnrollmentElectionStatus electionStatus);
    }

    public interface DependentCountStage {
        /**
         * <p>Dependents covered under this enrollment today. Counts the same dependents <code>premium_in_cents</code> is priced for, so a dependent whose termination is dated in the future still counts.</p>
         */
        _FinalStage dependentCount(int dependentCount);
    }

    public interface _FinalStage {
        PlanYearEnrollment build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Your own reference id for this employee, as you supplied it. Null when you have not set one.</p>
         */
        _FinalStage employeeExternalReferenceId(Optional<String> employeeExternalReferenceId);

        _FinalStage employeeExternalReferenceId(String employeeExternalReferenceId);

        _FinalStage employeeExternalReferenceId(Nullable<String> employeeExternalReferenceId);

        /**
         * <p>Where the member's coverage stands: <code>Coverage Upcoming</code>, <code>Coverage Effective</code>, <code>Coverage Ended</code>, or <code>Cancelled</code>. Null unless the member enrolled.</p>
         * <ul>
         * <li><code>Coverage Upcoming</code> - Coverage Upcoming</li>
         * <li><code>Coverage Effective</code> - Coverage Effective</li>
         * <li><code>Coverage Ended</code> - Coverage Ended</li>
         * <li><code>Cancelled</code> - Cancelled</li>
         * </ul>
         */
        _FinalStage policyStatus(Optional<PlanYearEnrollmentPolicyStatus> policyStatus);

        _FinalStage policyStatus(PlanYearEnrollmentPolicyStatus policyStatus);

        _FinalStage policyStatus(Nullable<PlanYearEnrollmentPolicyStatus> policyStatus);

        /**
         * <p>The carrier for this enrollment: the individual-market carrier for an ICHRA plan, otherwise the benefit's own. Null when the benefit has no carrier.</p>
         */
        _FinalStage carrier(Optional<String> carrier);

        _FinalStage carrier(String carrier);

        _FinalStage carrier(Nullable<String> carrier);

        /**
         * <p>Chosen plan name, or null when unanswered/waived.</p>
         */
        _FinalStage plan(Optional<String> plan);

        _FinalStage plan(String plan);

        _FinalStage plan(Nullable<String> plan);

        /**
         * <p>Chosen coverage tier, or null when unanswered/waived.</p>
         */
        _FinalStage tier(Optional<String> tier);

        _FinalStage tier(String tier);

        _FinalStage tier(Nullable<String> tier);

        /**
         * <p>Monthly premium in cents for the chosen plan, dependents included. The plan's own cost, not the employer's share of it. Null when unanswered/waived.</p>
         */
        _FinalStage premiumInCents(Optional<Integer> premiumInCents);

        _FinalStage premiumInCents(Integer premiumInCents);

        _FinalStage premiumInCents(Nullable<Integer> premiumInCents);

        /**
         * <p>The employer's monthly share of <code>premium_in_cents</code>, in cents. Null when unanswered/waived.</p>
         */
        _FinalStage employerContributionInCents(Optional<Integer> employerContributionInCents);

        _FinalStage employerContributionInCents(Integer employerContributionInCents);

        _FinalStage employerContributionInCents(Nullable<Integer> employerContributionInCents);

        /**
         * <p>What the employee is deducted monthly, in cents: <code>premium_in_cents</code> less <code>employer_contribution_in_cents</code>, floored at zero. Null when unanswered/waived.</p>
         */
        _FinalStage employeeDeductionInCents(Optional<Integer> employeeDeductionInCents);

        _FinalStage employeeDeductionInCents(Integer employeeDeductionInCents);

        _FinalStage employeeDeductionInCents(Nullable<Integer> employeeDeductionInCents);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements MemberIdStage,
                    EmployeeIdStage,
                    MemberFirstNameStage,
                    MemberLastNameStage,
                    ElectionStatusStage,
                    DependentCountStage,
                    _FinalStage {
        private String memberId;

        private String employeeId;

        private String memberFirstName;

        private String memberLastName;

        private PlanYearEnrollmentElectionStatus electionStatus;

        private int dependentCount;

        private Optional<Integer> employeeDeductionInCents = Optional.empty();

        private Optional<Integer> employerContributionInCents = Optional.empty();

        private Optional<Integer> premiumInCents = Optional.empty();

        private Optional<String> tier = Optional.empty();

        private Optional<String> plan = Optional.empty();

        private Optional<String> carrier = Optional.empty();

        private Optional<PlanYearEnrollmentPolicyStatus> policyStatus = Optional.empty();

        private Optional<String> employeeExternalReferenceId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PlanYearEnrollment other) {
            memberId(other.getMemberId());
            employeeId(other.getEmployeeId());
            employeeExternalReferenceId(other.getEmployeeExternalReferenceId());
            memberFirstName(other.getMemberFirstName());
            memberLastName(other.getMemberLastName());
            electionStatus(other.getElectionStatus());
            policyStatus(other.getPolicyStatus());
            dependentCount(other.getDependentCount());
            carrier(other.getCarrier());
            plan(other.getPlan());
            tier(other.getTier());
            premiumInCents(other.getPremiumInCents());
            employerContributionInCents(other.getEmployerContributionInCents());
            employeeDeductionInCents(other.getEmployeeDeductionInCents());
            return this;
        }

        /**
         * <p>Our id for the person (<code>mbr_&lt;...&gt;</code>). Stable across every employer they work for.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("member_id")
        public EmployeeIdStage memberId(@NotNull String memberId) {
            this.memberId = Objects.requireNonNull(memberId, "memberId must not be null");
            return this;
        }

        /**
         * <p>Our id for this person's employment with this employer (<code>empl_&lt;...&gt;</code>). A person who leaves and is rehired has two.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("employee_id")
        public MemberFirstNameStage employeeId(@NotNull String employeeId) {
            this.employeeId = Objects.requireNonNull(employeeId, "employeeId must not be null");
            return this;
        }

        /**
         * <p>The member's first name.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("member_first_name")
        public MemberLastNameStage memberFirstName(@NotNull String memberFirstName) {
            this.memberFirstName = Objects.requireNonNull(memberFirstName, "memberFirstName must not be null");
            return this;
        }

        /**
         * <p>The member's last name.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("member_last_name")
        public ElectionStatusStage memberLastName(@NotNull String memberLastName) {
            this.memberLastName = Objects.requireNonNull(memberLastName, "memberLastName must not be null");
            return this;
        }

        /**
         * <p>Where the member stands: <code>Enrolled</code> or <code>Waived</code> if they answered, otherwise <code>Pending</code> while their enrollment window is open and <code>Expired</code> once it has closed.</p>
         * <ul>
         * <li><code>Enrolled</code> - Enrolled</li>
         * <li><code>Waived</code> - Waived</li>
         * <li><code>Pending</code> - Pending</li>
         * <li><code>Expired</code> - Expired</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("election_status")
        public DependentCountStage electionStatus(@NotNull PlanYearEnrollmentElectionStatus electionStatus) {
            this.electionStatus = Objects.requireNonNull(electionStatus, "electionStatus must not be null");
            return this;
        }

        /**
         * <p>Dependents covered under this enrollment today. Counts the same dependents <code>premium_in_cents</code> is priced for, so a dependent whose termination is dated in the future still counts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("dependent_count")
        public _FinalStage dependentCount(int dependentCount) {
            this.dependentCount = dependentCount;
            return this;
        }

        /**
         * <p>What the employee is deducted monthly, in cents: <code>premium_in_cents</code> less <code>employer_contribution_in_cents</code>, floored at zero. Null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage employeeDeductionInCents(Nullable<Integer> employeeDeductionInCents) {
            if (employeeDeductionInCents.isNull()) {
                this.employeeDeductionInCents = null;
            } else if (employeeDeductionInCents.isEmpty()) {
                this.employeeDeductionInCents = Optional.empty();
            } else {
                this.employeeDeductionInCents = Optional.of(employeeDeductionInCents.get());
            }
            return this;
        }

        /**
         * <p>What the employee is deducted monthly, in cents: <code>premium_in_cents</code> less <code>employer_contribution_in_cents</code>, floored at zero. Null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage employeeDeductionInCents(Integer employeeDeductionInCents) {
            this.employeeDeductionInCents = Optional.ofNullable(employeeDeductionInCents);
            return this;
        }

        /**
         * <p>What the employee is deducted monthly, in cents: <code>premium_in_cents</code> less <code>employer_contribution_in_cents</code>, floored at zero. Null when unanswered/waived.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "employee_deduction_in_cents", nulls = Nulls.SKIP)
        public _FinalStage employeeDeductionInCents(Optional<Integer> employeeDeductionInCents) {
            this.employeeDeductionInCents = employeeDeductionInCents;
            return this;
        }

        /**
         * <p>The employer's monthly share of <code>premium_in_cents</code>, in cents. Null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage employerContributionInCents(Nullable<Integer> employerContributionInCents) {
            if (employerContributionInCents.isNull()) {
                this.employerContributionInCents = null;
            } else if (employerContributionInCents.isEmpty()) {
                this.employerContributionInCents = Optional.empty();
            } else {
                this.employerContributionInCents = Optional.of(employerContributionInCents.get());
            }
            return this;
        }

        /**
         * <p>The employer's monthly share of <code>premium_in_cents</code>, in cents. Null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage employerContributionInCents(Integer employerContributionInCents) {
            this.employerContributionInCents = Optional.ofNullable(employerContributionInCents);
            return this;
        }

        /**
         * <p>The employer's monthly share of <code>premium_in_cents</code>, in cents. Null when unanswered/waived.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "employer_contribution_in_cents", nulls = Nulls.SKIP)
        public _FinalStage employerContributionInCents(Optional<Integer> employerContributionInCents) {
            this.employerContributionInCents = employerContributionInCents;
            return this;
        }

        /**
         * <p>Monthly premium in cents for the chosen plan, dependents included. The plan's own cost, not the employer's share of it. Null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage premiumInCents(Nullable<Integer> premiumInCents) {
            if (premiumInCents.isNull()) {
                this.premiumInCents = null;
            } else if (premiumInCents.isEmpty()) {
                this.premiumInCents = Optional.empty();
            } else {
                this.premiumInCents = Optional.of(premiumInCents.get());
            }
            return this;
        }

        /**
         * <p>Monthly premium in cents for the chosen plan, dependents included. The plan's own cost, not the employer's share of it. Null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage premiumInCents(Integer premiumInCents) {
            this.premiumInCents = Optional.ofNullable(premiumInCents);
            return this;
        }

        /**
         * <p>Monthly premium in cents for the chosen plan, dependents included. The plan's own cost, not the employer's share of it. Null when unanswered/waived.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "premium_in_cents", nulls = Nulls.SKIP)
        public _FinalStage premiumInCents(Optional<Integer> premiumInCents) {
            this.premiumInCents = premiumInCents;
            return this;
        }

        /**
         * <p>Chosen coverage tier, or null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage tier(Nullable<String> tier) {
            if (tier.isNull()) {
                this.tier = null;
            } else if (tier.isEmpty()) {
                this.tier = Optional.empty();
            } else {
                this.tier = Optional.of(tier.get());
            }
            return this;
        }

        /**
         * <p>Chosen coverage tier, or null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage tier(String tier) {
            this.tier = Optional.ofNullable(tier);
            return this;
        }

        /**
         * <p>Chosen coverage tier, or null when unanswered/waived.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "tier", nulls = Nulls.SKIP)
        public _FinalStage tier(Optional<String> tier) {
            this.tier = tier;
            return this;
        }

        /**
         * <p>Chosen plan name, or null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage plan(Nullable<String> plan) {
            if (plan.isNull()) {
                this.plan = null;
            } else if (plan.isEmpty()) {
                this.plan = Optional.empty();
            } else {
                this.plan = Optional.of(plan.get());
            }
            return this;
        }

        /**
         * <p>Chosen plan name, or null when unanswered/waived.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage plan(String plan) {
            this.plan = Optional.ofNullable(plan);
            return this;
        }

        /**
         * <p>Chosen plan name, or null when unanswered/waived.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "plan", nulls = Nulls.SKIP)
        public _FinalStage plan(Optional<String> plan) {
            this.plan = plan;
            return this;
        }

        /**
         * <p>The carrier for this enrollment: the individual-market carrier for an ICHRA plan, otherwise the benefit's own. Null when the benefit has no carrier.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage carrier(Nullable<String> carrier) {
            if (carrier.isNull()) {
                this.carrier = null;
            } else if (carrier.isEmpty()) {
                this.carrier = Optional.empty();
            } else {
                this.carrier = Optional.of(carrier.get());
            }
            return this;
        }

        /**
         * <p>The carrier for this enrollment: the individual-market carrier for an ICHRA plan, otherwise the benefit's own. Null when the benefit has no carrier.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage carrier(String carrier) {
            this.carrier = Optional.ofNullable(carrier);
            return this;
        }

        /**
         * <p>The carrier for this enrollment: the individual-market carrier for an ICHRA plan, otherwise the benefit's own. Null when the benefit has no carrier.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "carrier", nulls = Nulls.SKIP)
        public _FinalStage carrier(Optional<String> carrier) {
            this.carrier = carrier;
            return this;
        }

        /**
         * <p>Where the member's coverage stands: <code>Coverage Upcoming</code>, <code>Coverage Effective</code>, <code>Coverage Ended</code>, or <code>Cancelled</code>. Null unless the member enrolled.</p>
         * <ul>
         * <li><code>Coverage Upcoming</code> - Coverage Upcoming</li>
         * <li><code>Coverage Effective</code> - Coverage Effective</li>
         * <li><code>Coverage Ended</code> - Coverage Ended</li>
         * <li><code>Cancelled</code> - Cancelled</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage policyStatus(Nullable<PlanYearEnrollmentPolicyStatus> policyStatus) {
            if (policyStatus.isNull()) {
                this.policyStatus = null;
            } else if (policyStatus.isEmpty()) {
                this.policyStatus = Optional.empty();
            } else {
                this.policyStatus = Optional.of(policyStatus.get());
            }
            return this;
        }

        /**
         * <p>Where the member's coverage stands: <code>Coverage Upcoming</code>, <code>Coverage Effective</code>, <code>Coverage Ended</code>, or <code>Cancelled</code>. Null unless the member enrolled.</p>
         * <ul>
         * <li><code>Coverage Upcoming</code> - Coverage Upcoming</li>
         * <li><code>Coverage Effective</code> - Coverage Effective</li>
         * <li><code>Coverage Ended</code> - Coverage Ended</li>
         * <li><code>Cancelled</code> - Cancelled</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage policyStatus(PlanYearEnrollmentPolicyStatus policyStatus) {
            this.policyStatus = Optional.ofNullable(policyStatus);
            return this;
        }

        /**
         * <p>Where the member's coverage stands: <code>Coverage Upcoming</code>, <code>Coverage Effective</code>, <code>Coverage Ended</code>, or <code>Cancelled</code>. Null unless the member enrolled.</p>
         * <ul>
         * <li><code>Coverage Upcoming</code> - Coverage Upcoming</li>
         * <li><code>Coverage Effective</code> - Coverage Effective</li>
         * <li><code>Coverage Ended</code> - Coverage Ended</li>
         * <li><code>Cancelled</code> - Cancelled</li>
         * </ul>
         */
        @java.lang.Override
        @JsonSetter(value = "policy_status", nulls = Nulls.SKIP)
        public _FinalStage policyStatus(Optional<PlanYearEnrollmentPolicyStatus> policyStatus) {
            this.policyStatus = policyStatus;
            return this;
        }

        /**
         * <p>Your own reference id for this employee, as you supplied it. Null when you have not set one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage employeeExternalReferenceId(Nullable<String> employeeExternalReferenceId) {
            if (employeeExternalReferenceId.isNull()) {
                this.employeeExternalReferenceId = null;
            } else if (employeeExternalReferenceId.isEmpty()) {
                this.employeeExternalReferenceId = Optional.empty();
            } else {
                this.employeeExternalReferenceId = Optional.of(employeeExternalReferenceId.get());
            }
            return this;
        }

        /**
         * <p>Your own reference id for this employee, as you supplied it. Null when you have not set one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage employeeExternalReferenceId(String employeeExternalReferenceId) {
            this.employeeExternalReferenceId = Optional.ofNullable(employeeExternalReferenceId);
            return this;
        }

        /**
         * <p>Your own reference id for this employee, as you supplied it. Null when you have not set one.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "employee_external_reference_id", nulls = Nulls.SKIP)
        public _FinalStage employeeExternalReferenceId(Optional<String> employeeExternalReferenceId) {
            this.employeeExternalReferenceId = employeeExternalReferenceId;
            return this;
        }

        @java.lang.Override
        public PlanYearEnrollment build() {
            return new PlanYearEnrollment(
                    memberId,
                    employeeId,
                    employeeExternalReferenceId,
                    memberFirstName,
                    memberLastName,
                    electionStatus,
                    policyStatus,
                    dependentCount,
                    carrier,
                    plan,
                    tier,
                    premiumInCents,
                    employerContributionInCents,
                    employeeDeductionInCents,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}

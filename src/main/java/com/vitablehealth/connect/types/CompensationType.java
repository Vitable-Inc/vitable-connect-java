/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CompensationType {
    public static final CompensationType SALARY = new CompensationType(Value.SALARY, "Salary");

    public static final CompensationType HOURLY = new CompensationType(Value.HOURLY, "Hourly");

    private final Value value;

    private final String string;

    CompensationType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CompensationType && this.string.equals(((CompensationType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SALARY:
                return visitor.visitSalary();
            case HOURLY:
                return visitor.visitHourly();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CompensationType valueOf(String value) {
        switch (value) {
            case "Salary":
                return SALARY;
            case "Hourly":
                return HOURLY;
            default:
                return new CompensationType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        SALARY,

        HOURLY,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitSalary();

        T visitHourly();

        T visitUnknown(String unknownType);
    }
}

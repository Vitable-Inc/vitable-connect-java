/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class EligibilityRuleCompensationType {
    public static final EligibilityRuleCompensationType SALARY =
            new EligibilityRuleCompensationType(Value.SALARY, "Salary");

    public static final EligibilityRuleCompensationType HOURLY =
            new EligibilityRuleCompensationType(Value.HOURLY, "Hourly");

    public static final EligibilityRuleCompensationType ALL = new EligibilityRuleCompensationType(Value.ALL, "All");

    private final Value value;

    private final String string;

    EligibilityRuleCompensationType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof EligibilityRuleCompensationType
                        && this.string.equals(((EligibilityRuleCompensationType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SALARY:
                return visitor.visitSalary();
            case HOURLY:
                return visitor.visitHourly();
            case ALL:
                return visitor.visitAll();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static EligibilityRuleCompensationType valueOf(String value) {
        switch (value) {
            case "Salary":
                return SALARY;
            case "Hourly":
                return HOURLY;
            case "All":
                return ALL;
            default:
                return new EligibilityRuleCompensationType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        SALARY,

        HOURLY,

        ALL,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitSalary();

        T visitHourly();

        T visitAll();

        T visitUnknown(String unknownType);
    }
}

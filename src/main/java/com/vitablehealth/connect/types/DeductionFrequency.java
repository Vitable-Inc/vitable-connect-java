/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DeductionFrequency {
    public static final DeductionFrequency SEMI_MONTHLY = new DeductionFrequency(Value.SEMI_MONTHLY, "semi_monthly");

    public static final DeductionFrequency WEEKLY = new DeductionFrequency(Value.WEEKLY, "weekly");

    public static final DeductionFrequency BI_WEEKLY = new DeductionFrequency(Value.BI_WEEKLY, "bi_weekly");

    public static final DeductionFrequency MONTHLY = new DeductionFrequency(Value.MONTHLY, "monthly");

    private final Value value;

    private final String string;

    DeductionFrequency(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof DeductionFrequency && this.string.equals(((DeductionFrequency) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SEMI_MONTHLY:
                return visitor.visitSemiMonthly();
            case WEEKLY:
                return visitor.visitWeekly();
            case BI_WEEKLY:
                return visitor.visitBiWeekly();
            case MONTHLY:
                return visitor.visitMonthly();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DeductionFrequency valueOf(String value) {
        switch (value) {
            case "semi_monthly":
                return SEMI_MONTHLY;
            case "weekly":
                return WEEKLY;
            case "bi_weekly":
                return BI_WEEKLY;
            case "monthly":
                return MONTHLY;
            default:
                return new DeductionFrequency(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        WEEKLY,

        BI_WEEKLY,

        SEMI_MONTHLY,

        MONTHLY,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitWeekly();

        T visitBiWeekly();

        T visitSemiMonthly();

        T visitMonthly();

        T visitUnknown(String unknownType);
    }
}

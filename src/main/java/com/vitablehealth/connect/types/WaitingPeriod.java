/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vitablehealth.connect.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class WaitingPeriod {
    public static final WaitingPeriod SIXTY_DAYS = new WaitingPeriod(Value.SIXTY_DAYS, "SIXTY_DAYS");

    public static final WaitingPeriod THIRTY_DAYS = new WaitingPeriod(Value.THIRTY_DAYS, "THIRTY_DAYS");

    public static final WaitingPeriod FIRST_OF_FOLLOWING_MONTH =
            new WaitingPeriod(Value.FIRST_OF_FOLLOWING_MONTH, "FIRST_OF_FOLLOWING_MONTH");

    private final Value value;

    private final String string;

    WaitingPeriod(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof WaitingPeriod && this.string.equals(((WaitingPeriod) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SIXTY_DAYS:
                return visitor.visitSixtyDays();
            case THIRTY_DAYS:
                return visitor.visitThirtyDays();
            case FIRST_OF_FOLLOWING_MONTH:
                return visitor.visitFirstOfFollowingMonth();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static WaitingPeriod valueOf(String value) {
        switch (value) {
            case "SIXTY_DAYS":
                return SIXTY_DAYS;
            case "THIRTY_DAYS":
                return THIRTY_DAYS;
            case "FIRST_OF_FOLLOWING_MONTH":
                return FIRST_OF_FOLLOWING_MONTH;
            default:
                return new WaitingPeriod(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        FIRST_OF_FOLLOWING_MONTH,

        THIRTY_DAYS,

        SIXTY_DAYS,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitFirstOfFollowingMonth();

        T visitThirtyDays();

        T visitSixtyDays();

        T visitUnknown(String unknownType);
    }
}
